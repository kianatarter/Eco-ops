this area uses d2 and three js for visualizing the inner surface of a sphere with data from associated file stars.csv

have a test in here that makes multiple spheres around central point in three js obj.camera(0,0,0) should showc

obj.camera(0,0,0)   >> obj.camera(0,0,0, timestamp)  by making use of timestamps from transactions like eco ops checkins. 
